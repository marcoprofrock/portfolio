# CLAUDE.md — Portfolio Project Notes

## Project Overview
Personal portfolio for Marco Pröfrock — Creative at Scholz & Friends, Stuttgart.
Hosted on GitHub Pages: https://github.com/marcoprofrock/portfolio.git

## Tech Stack
- Single-page HTML (`index.html`) with inline CSS/JS
- No build tools, no frameworks
- Local fonts (`Inter` via `@font-face`, preloaded)
- Google Analytics GA4 (consent-based, tag G-9G9TJLTWJ8)
- GitHub Pages hosting

## Key Files
- `index.html` — Main portfolio page (timeline intro, projects, awards, photo gallery, Mac chat)
- `io/index.html` — Hidden SP.IO application page with permanent side-by-side chat
- `intro.html` — Reference prototype for timeline animation (not linked)
- `privacy.html` — GDPR privacy policy
- `500ls/index.html` — **Standalone 500l/s case-study page** for the ADCE Student Award submission (`https://www.marco.ad/500ls`). NOT the old portfolio clone (that was removed when the tester went live in `index.html`); this is a fresh, self-contained single-file page. **Light-only** (no dark mode / no theme toggle) — the whole page background is brandbook **Cloud Dancer `#F0EEE9`** (same as the tester), text ink `#16160f`. Embeds the typeface tester + a 13-spring colour legend; `noindex,nofollow` (robots.txt Disallow `/500ls/`, kept out of sitemap). Loads GA4 directly (NO cookie banner) + fires a custom `adce_case_view` event. Not linked from `index.html`. (See Architecture → "500l/s Case Study".)
- `assets/fonts/500ls/500ls-01…13.otf` — The 13 final 500l/s display cuts (one per Stuttgart spring), `@font-face`'d as families `ls01…ls13`
- `assets/05_500ls/waves/01…13.svg` — Clean, lightweight web copies of the 13 data-derived spring waveforms (single `<polygon>`, integer coords, `viewBox 0 0 3000 500`, ~6 KB each), numbered by cut. Generated from the source SVGs in `assets/05_500ls/SVG_Wellen/` (left untracked, descriptive names). Used by the `/500ls/` "Wave lab" section. Source chemistry: `assets/05_500ls/02_Anhang_A_…Mineralquellen.csv` (untracked).
- `assets/08_Photography/index.json` — Photo gallery manifest (regenerated from disk)
- `assets/11_MiniMac/03_isolated_html/minimac-animatio-emotion.html` — Mini Mac animation reference

## Architecture
### Intro Timeline
- Scroll-driven animation (500vh scroll container)
- Pixel-art Mac character (`#chatTrigger`) starts centered, moves right, shrinks to corner
- 3 stations (2021, 2023, 2024) fade in/out at scroll progress milestones
- After timeline ends (progress >= 0.97), Mac gets `.settled` class → becomes clickable chat trigger

### Mini Mac Walk System
- After settling, Mac idles for 5s then walks to opposite side of screen
- Walk animation: sit → stand → turn → side-walk → turn → sit
- 8 face expressions (happy, wink, surprised, bored, cheeky, sideEye, excited, dead)
- Speech bubbles with "click me" messages
- Clickable on both left and right side → opens Mac chat overlay
- `MutationObserver` watches for `.settled` class to activate/deactivate walk system

### Mac Chat Overlay
- Retro 1984 Macintosh UI with Q&A pool
- Free-text input forwards to Formspree (hello@marco.ad)
- Contact info shown after 2+ questions

### Photo Gallery
- Loaded from `index.json` manifest
- Fisher-Yates shuffle on load
- Horizontal scroll track with lightbox

### 500l/s Typeface Tester (live in `index.html`)
- Lives in the live `index.html` — a `<section class="tt">` panel inside the `#p-500ls` project block (collapsed `<details>`, so it shows when the project is expanded). Module CSS = `<style id="tt-style">` before `</head>`; logic = `<script id="tt-script">` before `</body>` (guards `if(!preview)return`). Relative asset paths (`assets/fonts/500ls/…`). The old standalone `/500ls/` page was removed after going live.
- Source of truth for the behaviour is `33_BA_500ls/12_BA_ABGABE_klein/07_Anhang_G_Headline_Extended_Script_randomizer.jsx` (the Illustrator randomizer), NOT just the thesis. Keep them in sync if cuts/colors change.
- 13 cuts = 13 Stuttgart springs, each with a fixed colour. Cut→HEX (from the script): 01 #EDEC3F · 02 #9DE89F · 03 #62C2B1 · 04 #FFC03F · 05 #FFAE3B · 06 #FFE552 · 07 #FCCF61 · 08 #D4E458 · 09 #0076AD · 10 #00ABC2 · 11 #3F526F · 12 #235BA8 · 13 #008DB1. Fonts are `@font-face` families `ls01…ls13` → `assets/fonts/500ls/500ls-NN.otf`.
- **Dachmarke (mix):** each glyph gets a random cut **+ that cut's colour as a pair**, never the same cut twice in a row. **Einzelschnitt:** one cut+colour for all glyphs (caption shows spring + HEX).
- **Baseline wave:** per-glyph `translateY(±wf · pct em)`, slider 0–100 % (= Illustrator `fontSize · pct`). **Defaults to 15 %** on load.
- **Weight (two glyph sets, NOT real case):** the font's "uppercase" and "lowercase" codepoints are two distinct glyph designs; the **lowercase-codepoint set is the heavier one** (measured fill ratio 0.806 vs 0.672). The `#ttCase` select (kept that id internally) is labelled **Weight** with options **As typed** (`none`) / **Light** (`upper` = finer set) / **Heavy** (`lower` = thicker set) / **Mixed** (`random`, per-glyph stable via `rolls[].uc`). Mixing both sets gives a more diverse texture — that's why default = **Mixed**.
- UI chrome is **English** (the spring names stay in German). Controls: text field, Cut select ("Mixed — all 13 cuts" = Dachmarke + the 13 springs under "Single cut (spring)"), Weight select (see above), Baseline wave slider, "↻ Shuffle". Mix caption: "13 cuts · randomly mixed · no cut twice in a row". On each page load the text field is seeded with a **random word from a mineral/ion `POOL`** (Calcium, Magnesium, Sodium, Potassium, Chloride, Sulfate, Bicarbonate, Carbonate, Fluoride, Lithium, Strontium — the constituents that occur in the springs; HTML fallback value "Calcium"). The old explanatory `.tt-sub` paragraph was removed (the case film already explains it; the `.tt-sub` CSS rule is now unused). Per-position randomness is stored in a `rolls[]` array so typing/sliding doesn't reshuffle existing glyphs; Shuffle regenerates it.
- Default load = mixed cuts + colour + Mixed weight + baseline wave 15 %. Tester window background = brandbook **Cloud Dancer** `#F0EEE9` (genuine PANTONE 11-4201 TCX off-white; brandbook prints it as #FFFFFF but pure white would vanish on the light theme) on BOTH dark/light themes.
- Ported into the live `index.html` by extracting the `tt-style` / `<section class="tt">` / `tt-script` blocks from the prototype and injecting them (before `</head>`, into `#p-500ls` between the `.info` close and `.cc-hint`, before `</body>`), rewriting `/assets`→`assets`.

### 500l/s Case Study (`/500ls/`)
- `500ls/index.html` is a **standalone case-study landing page** for the ADCE Student Award submission. Separate from the live tester in `index.html` (above) — it reuses the same `.tt` widget + 13 `@font-face` but exists as its own jury-facing page so the submission can link a short, distraction-free URL.
- Self-contained single-file HTML, **root-relative `/assets`** paths (it lives in a subfolder). Reuses the main design system's type/spacing/components (Inter preload, CSS vars, `.wrap` container) but is **light-only**: `:root` is the light palette with `--bg:#F0EEE9` (Cloud Dancer) + `--fg:#16160f`; the former dark vars, the `html.light` block, the theme-toggle button/CSS/JS and the anti-FOUC head script were all removed. Logos are black (`brightness(0)`); surfaces are faint-dark tints on the cream; the springs/awards cards stay white so they read against the cream page.
- The sticky header `.top` is full-width (translucent Cloud Dancer blur) but its inner content sits in a `.top-inner` capped at `--content-max` and centred — so the back-link/“Try the typeface ↓” line up flush-left with the hero + all section content (the `.wrap` gutter). NB: `.cs-hero` must set `padding-top`/`padding-bottom` (NOT the `padding` shorthand) so it doesn't clobber `.wrap`'s horizontal gutter.
- Sections top→bottom: minimal header (← back to `/` + "Try the typeface ↓", no toggle) → hero (colour logo + tagline H1 + Client/Year + **"Project: Bachelor thesis · solo"** + TDC-Annual honour + recognised-at) → "The idea" (Problem/Idea/Solution **verbatim from `#p-500ls`**, 3-col desktop) → case film (lite-yt `JCJF8uDo0tQ`, "press CC") → **interleaved media flow** under one "Key visuals" heading → credits + awards → footer.
- **Media order (interactive elements interleaved between the images, per Marco):** case film → board `04` → **Wave lab** (`#wavelab`, data→wave→grid→letter, see below) → 2-up `02`/`01` → **specimen video** → gallery group 2 (board `06` + 2-up `05`/`07`) → **type tester** → gallery group 3 (board `08` + 2-up `09`/`10`) → **springs legend** (the sources/colours overview). All live in one `#visuals` section; the old separate "The parametric system" heading + lead was dropped (the springs block self-titles). The header's "Try the typeface ↓" anchor (`#tester`) now lives on the `.tt` tester element. Each `.gallery` group has `margin-top` so the interleaved blocks space evenly.
- **Wave lab (`#wavelab`, `.wavelab` + `wavelab-script`):** interactive "From data to grid" section right after the board, explaining the board's derivation with the **real** assets — no free sliders (Marco's call: only the 13 actual springs). 13 colour chips select a spring. **Default view = 3 stacked waves** (upper/middle/lower) of that spring's **real waveform** (clean `assets/05_500ls/waves/NN.svg` polygon recoloured to the exact brand hex), because the **vertical spacing between the waves is itself a datapoint** (Sulfate). Data panel uses the **canonical brandbook mapping** (from `Beispiel_SVG/beispiel-beschriftung.svg`): **Na→Frequency**, **Cl→Amplitude**, **Ca→Crest shape (expOut)**, **Mg→Trough shape (expIn)**, **CO₂→Band thickness**, **SO₄→Wave spacing (between the 3 waves)**, **Temp→Colour**. **Hovering a data row annotates that feature on the real wave** (frequency ticks, amplitude/spacing/thickness brackets, crest/trough edge highlights). Annotations are deliberately **filigree** to match the brandbook (`.wl-ann` strokes `#2b2b2a`, ~1px `vector-effect:non-scaling-stroke`, small mono labels). **"Stack into grid"** tiles the wave onto a **DIN-A portrait sheet** (viewBox `3000×4243`, ratio 1:√2 — the brandbook always lays the grid on DIN-A) and overlays the raster (verticals at every peak & trough via a threshold peakdet, horizontals under every 3rd wave). **"Drop a letter"** drops the spring's real cut glyph (`lsNN`), scaled via `getBBox` to be **exactly 6 grid-rows tall** (letters are 3 cols × 6 rows in the system; W/M are 5 wide, i/! are 1) and snapped to the grid. Letters are NOT morphed (static fonts); each cut genuinely fits its own spring's grid. Reference art: `assets/05_500ls/Beispiel_SVG/` (untracked).
- **Recognition (deliberately tier-free — don't anchor the jury low):** the **only** named honour is the highest one — `.cs-honor` "**The World's Best Typography — Selected for the TDC Annual 2026**" (TDC logo), shown **both** in the hero (top) and in the bottom "Recognition" block. The specific tiers (Bronze / Merit / Certificate of Typographic Excellence) were **removed** on Marco's call; instead a tier-free `.cs-reco` "**Recognised at**" row shows the three org logos (ADC New York · TDC · ADC Germany). JSON-LD `award[]` simplified to match. Credits list uses **Scope = "Bachelor thesis — … created entirely solo"** (no "Role/Art Direction").
- **Case-film poster:** uses YouTube `sddefault.jpg` (+ `hqdefault` fallback), NOT `maxresdefault` — the case film is ~4:3, so the 16:9 maxres thumb has black pillarbox bars baked in; the 4:3 sd/hq thumb + `object-fit:cover` crops cleanly to the 16:9 frame with no bars.
- `.springs` legend: 13 cards on a Cloud-Dancer panel, each showing the spring's initial in its own cut font + colour, plus `Cut NN`, name, swatch + HEX. Built in `tt-script` from the same `CUTS` array (single source of truth).
- **Type-specimen video** (`.specimen`, interleaved in the `#visuals` flow): `assets/05_500ls/500l_s_Showcase_1080x1920_60s_1.mp4` (1080×1920 9:16, 60 s, ~4.4 MB) inline `<video muted playsinline preload="auto">`, no controls/play button. CSS-cropped to **4:5** (`.specimen-frame aspect-ratio:4/5`, `object-fit:cover`, `transform:scale(1.04)`); frame background is Cloud-Dancer `#F0EEE9` (not `#000`) so no black edge-flashes. **Playback is scrub-only on mouse devices:** the `<video>` has NO `autoplay`/`loop` — on fine-pointer it stays **paused on a frame** and only advances WHILE the cursor slides over the video's vertical band (full screen width, any X); Y→`currentTime` (top=0, bottom=end); leaving the band **freezes it on the current frame** (never auto-plays/loops). Thin `.specimen-scrub` line + "· move up / down to scrub" hint shown via `.is-scrubbing` (gated on `.scrubbable`). Touch/coarse-pointer devices instead just **loop** (JS sets `v.loop=true` + plays, paused off-screen via IntersectionObserver). NB: scrubbing needs HTTP **Range** support — fine on GitHub Pages; the local `python -m http.server` does NOT do ranges (video stays non-seekable in that preview; the bundled range-capable `/tmp` serve script does).
- Tester default state is **pinned on load** (`mix` / `random` weight / `30%` wave) in `tt-script` so browser form-state restoration can't drift the controls (HTML attrs already say so; JS enforces it). This pin is specific to the case-study page's tt-script.
- **Baseline-wave amplitude is halved vs the live index.html tester** (Marco found 100% too chaotic — "up to 50% is aesthetic"): the case-study `tt-script` uses `amp=(+wave.value)/200` (not `/100`), so the slider still reads 0–100% but **100% now equals the old 50% amplitude**; the whole range stays in the calm/aesthetic zone. Default slider value is **30%** (= the old 15% amplitude), so the on-load wave looks identical to before. Only the case-study page changed; the live tester in `index.html` is untouched (`/100`, default 15%).
- **SEO/Analytics (Marco's decisions):** `noindex,nofollow` + self-canonical + robots.txt Disallow `/500ls/` + NOT in sitemap (avoids duplicate content with `#p-500ls`). GA4 (`G-9G9TJLTWJ8`) loads **directly in `<head>` with NO cookie banner** (deliberate for this submission page) and fires a custom `gtag('event','adce_case_view',{project:'500ls',source:'adce_student_award'})` on load so jury visits are a distinct event; lite-yt clicks fire `video_play`.
- Hand-authored artefact (not generated from `index.html`); edit it directly. `index.html` is untouched by this page.

## Conventions
- Auto-push after each prompt (unless told otherwise)
- Git identity: Marco Pröfrock <hello@marco.ad>
- CLAUDE.md updated and pushed with every change
- German is the user's language; code comments in English

## GDPR Compliance
- No cookies before user consent (cookie banner with overlay)
- GA4 loads only after consent, stored in localStorage
- Local fonts (no Google Fonts CDN)
- No external tracking scripts (ipapi.co removed)
- Privacy policy at `/privacy.html`, linked from footer

## Session Log
- **2026-05-05**: Integrated Mini Mac walk animation system from `minimac-animatio-emotion.html` into portfolio. Mac now walks between left/right sides when idle, shows speech bubbles, changes expressions. Clickable on both sides to open chat. Created CLAUDE.md.
- **2026-05-05**: Extended timeline scroll from 500vh→900vh for longer dwell on stations. Spread station ranges wider (10–35%, 35–58%, 58–80%). Made year labels much larger (28–48px) and positioned them between Mac and GIF on desktop.
- **2026-05-05**: Fixed broken intro — smart/curly quotes (U+2018/2019/201C/201D) had leaked into JS causing silent parse error. Replaced all with straight quotes in script blocks.
- **2026-05-06**: Style adaptation — increased spacing across all sections (projects, about, CV, awards table, photo strip) to match airy intro aesthetic. Station fly-in/fly-out: now slides from top (in) and down (out) with scale 88%→100%→88% for scroll feedback. Extended station dwell time.
- **2026-05-19**: ADC Talent Award 2026 results — 3 of 4 shortlists upgraded to Nagel (color tbd), 1 stays as Auszeichnung. Updated project award badges + credits and awards table: 500l/s (1× Nagel gewonnen), Windkulturerbe (2× Nagel gewonnen), U-Turn (1× Auszeichnung). Awards table row now reads "Nagel · Nagel · Nagel · Auszeichnung".
- **2026-05-29**: Added talks entry (24.07.2026 Pop Up ADC Creative Club, Areal Süd, Windkulturerbe, Upcoming badge). Sorted talks & awards tables newest-first (ADC Talent 2026 on top). Built hidden light-mode TEST page at `/light/` (noindex, clone of index.html with absolute /assets paths). Adds a subtle pill toggle centered in the header that switches `html.light`; light mode overrides CSS vars (bg #fff, text #0e0e0e) and flips white award/project SVG logos to black via `filter: brightness(0)`. Choice persists in localStorage; default stays dark. Main index.html unchanged. Light mode also strips all drop shadows (box-shadow/filter drop-shadow) — white speech bubbles get a 0.5px hairline border instead so they stay legible on white. Plan: `/light/` is the in-progress next version that will eventually REPLACE the live index.html; keep iterating there, leave index.html untouched until the swap.
- **2026-05-29**: `/light/` About section — Experience & Education now sit side-by-side in a `.cv-pair` 2-col grid on desktop (collapses to 1 col ≤720px) to cut the excessive desktop whitespace. Test page only.
- **2026-05-29**: `/light/` experiment — removed ALL divider lines (border-top/bottom) in CV rows, Memberships, Awards table and Talks table; rows now separated by padding only. Override block appended at end of main `<style>` so it wins over the mobile media-query borders too. Test page only — pending Marco's verdict.
- **2026-06-01**: Took `/io/` page offline (git rm -r io → 404). Technical SEO layer on live `index.html` (0 visual changes): added canonical (https://www.marco.ad/), author meta, full Open Graph + Twitter `summary_large_image` (og:image = about portrait `/assets/06_about/02_c217dbac.webp`, .webp caveat noted — JPG 1200×630 to follow), JSON-LD Schema.org Person (jobTitle "Creative / Art Director", worksFor Scholz & Friends, alumniOf HdM + IADT, sameAs LinkedIn+Instagram from footer, award[] from awards table). Added `.visually-hidden` class + hidden H1 "Marco Pröfrock — Creative / Art Director, Stuttgart" at top of `<main>`; demoted visible "Work" H1→H2 (`.section-title` is class-based, optics unchanged) → now 1×H1, 3×H2. Created `sitemap.xml` (/, /privacy.html only) and `robots.txt` (disallow /light/ /oslo/, Sitemap ref). Added `noindex,nofollow` to `intro.html`. Alt-audit: filled 17 project gallery images with project-name alt; left 47 decorative alts empty (logos w/ adjacent labels or aria-hidden, station GIFs, YouTube thumbs already labeled via button aria-label).
- **2026-06-12**: ADC Talent Award 2026 final colors — all 3 Nägel are **Bronze**. Replaced "Nagel" → "Bronzener Nagel" in awards table row (now "Bronzener Nagel · Bronzener Nagel · Bronzener Nagel · Auszeichnung"), project credits (500l/s "1× Bronzener Nagel gewonnen", Windkulturerbe "2× Bronzener Nagel gewonnen") and JSON-LD award[]. Applied across index.html, light/ and oslo/. U-Turn stays Auszeichnung.
- **2026-06-18**: Awards wording simplified "Bronzener Nagel" → **"Bronze"** (table "Bronze · Bronze · Bronze · Auszeichnung", credits "1×/2× Bronze gewonnen", JSON-LD). **Light mode shipped LIVE as main page**: ported all light-mode features from `/light/` into `index.html` (early theme script in head, full `html.light` CSS override block + header `.theme-toggle` + toggle JS, `.cv-pair` Experience/Education side-by-side desktop grid, divider-line removal block) WITHOUT losing the SEO layer (canonical/OG/JSON-LD/hidden-H1/alt). Default stays dark, toggle persists in localStorage. Removed now-redundant `/light/` dir (git rm) and dropped its robots.txt Disallow (kept /oslo/). Verified via preview: light CSS applies (bg #fff, fg #0e0e0e), award logos brightness(0), dividers 0px, no station shadow, cv-pair 2-col desktop, toggle works, 1×H1, canonical present, noindex absent.
- **2026-06-19**: Light-mode contrast tweaks (live index.html). Mini Mac body recolored beige→black via `html.light #chatTrigger [fill="#d4c8a0"] { fill: #0e0e0e; }` (covers static + JS-built SVGs; CSS fill overrides presentation attr; orange screen untouched). Intro GIF station window borders bumped from 0.5px rgba(14,14,14,0.14) → 1px rgba(14,14,14,0.4) to match the vertical `#timeline-hairline`. Verified via preview computed styles.
- **2026-06-19**: Added interactive **500l/s typeface tester** at `/500ls/` (full clone of index.html with root-relative `/assets` paths + `noindex,nofollow`; robots.txt Disallow `/500ls/`). Bundled the 13 final OTF cuts from `33_BA_500ls/17_Design_Final/02_TypeFinal/260110_500l:s/` into `assets/fonts/500ls/500ls-01…13.otf` (~160 KB total), `@font-face`'d as families `ls01…ls13`. Tester is embedded inside the `#p-500ls` project block (that `<details>` is `open` by default on this page) as a `.tt` panel; module CSS/JS injected before `</head>`/`</body>`. Authoritative behaviour ported from `Anhang_G_Headline_Extended_Script_randomizer.jsx`: **Dachmarke (mix)** assigns each glyph a random cut + that cut's fixed spring colour (HEX from the script: 01 #EDEC3F … 13 #008DB1), with no same-cut twice in a row; **Einzelschnitt** locks one cut+colour; random **baseline-shift** slider 0–100 % = `translateY(±wf·pct em)` per glyph (the wave). Controls: text field, Schnitt select (Dachmarke + 13 springs), Schreibweise (Original/GROSS/klein/zufällig — case stable per glyph), Baseline-Welle slider, "Neu würfeln". Default load = mix + colour + straight baseline (wave off). The tester window sits on brandbook **Cloud Dancer** bg `#F0EEE9` on BOTH dark/light themes (brandbook prints it as #FFFFFF, but pure white would vanish on the light theme, so used the genuine 11-4201 TCX off-white). Live `index.html` is NOT linked to `/500ls/` yet — pending Marco's go to add a "Try the typeface →" link in the 500l/s project block. Verified via preview: fonts load, 0 consecutive repeats, colour/cut pairing correct, wave applies, Cloud Dancer bg both themes, no console errors. Follow-ups same day: localized the tester UI to English (kept German spring names); enlarged preview type ~1.7× (`.tt-preview` font-size clamp(40,8.5vw,104)→clamp(68,14.5vw,177)px, `.tt-stage` min-height clamp(120,22vw,240)→clamp(180,30vw,340)px).
- **2026-06-19**: Shipped the 500l/s **typeface tester LIVE in `index.html`**. Extracted the `tt-style` CSS (13 `@font-face` ls01…ls13), the `<section class="tt">` widget, and the `tt-script` JS from the `/500ls/` prototype and injected them into the live page (CSS before `</head>`, widget inside `#p-500ls` between `.info` close and `.cc-hint`, JS before `</body>`; `/assets`→`assets`). Removed the now-redundant standalone `/500ls/` page (`git rm -r`) and its robots.txt Disallow (kept `/oslo/`). Fonts already committed under `assets/fonts/500ls/`. SEO/light-mode/content untouched. Verified via preview before the swap: widget renders in #p-500ls, glyph spans get per-cut fonts (ls13…) + spring colours, ls01/ls13 `document.fonts` load, no console errors.
- **2026-06-19**: Light-mode Mini Mac body recolored from pure black `#0e0e0e` → **anthracite `#293133`** (RAL 7016) — black was too stark / lacked contrast on some lighter asset backgrounds. Same rule `html.light #chatTrigger [fill="#d4c8a0"] { fill: #293133; }`.
- **2026-06-19**: Built a **standalone 500l/s case-study page** at `/500ls/` for the **ADCE Student Award** submission (`https://www.marco.ad/500ls`) — recreating the path that was freed when the tester went live in `index.html`. Hand-authored single-file HTML (root-relative `/assets`) reusing the main design system + `html.light` toggle. Sections: minimal header → hero (colour logo + tagline + Client/Year/Role + award badges) → "The idea" (Problem/Idea/Solution copy verbatim from `#p-500ls`) → case film (lite-yt `JCJF8uDo0tQ`) → key-visuals gallery (`/assets/05_500ls/…`, lightbox) → "The parametric system" (the `.tt` tester carried over 1:1 + a new `.springs` 13-card legend built from the same `CUTS` array) → credits + awards → footer (hello@marco.ad + back-link). Per Marco's decisions: **GA4 without cookie banner** (loads `G-9G9TJLTWJ8` in `<head>`, fires custom `adce_case_view` event on load + `video_play` on the film); **noindex** (self-canonical, re-added robots.txt Disallow `/500ls/`, kept out of sitemap). Hardened the case-study tt-script to pin defaults on load (`mix`/`random`/`15%`). `index.html` (now hosting the live tester) left untouched. Verified via preview (served from a `/tmp` copy — the mounted volume is sandboxed away from the dev server): 13 fonts + Inter load, spring colour↔cut pairing correct, 0/200 consecutive-repeat violations, single-cut caption correct, wave-0 removes transforms, 13-card legend renders with cut-font glyphs, case-film thumbnail + gallery load, theme toggle persists dark↔light, responsive 375px (controls stack, springs grid 2-col, toggle hidden <460px), no console errors.
- **2026-06-19**: Added a **type-specimen video** to the `/500ls/` case study (in "The parametric system", between the lead and the tester). `assets/05_500ls/500l_s_Showcase_1080x1920_60s_1.mp4` (Marco's 60 s clip running through every glyph) embedded as an inline gif-style `<video autoplay muted loop playsinline>` (no controls), native 9:16, centered phone-format, not cropped. On mouse devices, **hovering scrubs** the clip by cursor Y (top=start → bottom=end), pausing while hovered and resuming the loop on leave (thin indicator line + "hover to scrub" hint); touch devices just loop. Pauses when off-screen (IntersectionObserver). Verified via a **range-capable** local test server (the stock `python -m http.server` doesn't do byte ranges, so the video reads as non-seekable there — but GitHub Pages serves ranges, so scrubbing works in production): autoplay+loop play in view / pause off-screen, hover-scrub maps top=0.2s · mid=30s · bottom=59.7s, resumes on leave, responsive (400×711 desktop / 293×520 mobile), no console errors.
- **2026-06-19**: `/500ls/` polish per Marco. (1) **Light-only, Cloud Dancer page bg:** removed dark mode entirely from the case study — dropped the theme-toggle button/CSS/JS, the anti-FOUC head script and the `html.light` block; set `:root` to the light palette with `--bg:#F0EEE9` + `--fg:#16160f`, folded the former light-mode overrides (logos `brightness(0)`, faint-dark surface tints, white award/spring cards) into the base. The whole page now sits on the tester's Cloud-Dancer off-white instead of pure white. (2) **Header alignment:** wrapped the sticky header's content in a `.top-inner` capped at `--content-max` + centred, so the back-link lines up flush-left with the hero/sections; also fixed `.cs-hero` to use `padding-top`/`padding-bottom` (the `padding` shorthand had zeroed `.wrap`'s horizontal gutter, pushing the hero to the viewport edge). (3) **Don't undersell:** removed "Role / Art Direction" → hero fact "Project: Bachelor thesis · solo" + credits "Scope: Bachelor thesis — concept, brand identity, type design & campaign, created entirely solo" (kept Supervision). (4) **Added recognition:** "The World's Best Typography — selected for the TDC Annual 2026" to the awards list (TDC logo) + JSON-LD `award[]`. (5) **Case-film bars:** swapped the poster from `maxresdefault` (16:9, baked pillarbox bars because the film is ~4:3) to `sddefault`+`hqdefault` (4:3) so `object-fit:cover` crops cleanly to 16:9 with no black bars. Verified via preview (range-capable `/tmp` server): page bg `#F0EEE9`, no toggle, all left edges aligned at the `.wrap` gutter (36px desktop / 20px mobile), poster bar-free, awards show the TDC Annual entry, hero/credits show the BA-thesis wording, no console errors.
- **2026-06-19**: `/500ls/` recognition + specimen refinements per Marco. (1) **TDC Annual to the top:** added the `.cs-honor` "The World's Best Typography — Selected for the TDC Annual 2026" prominently in the hero (it was only at the bottom before). (2) **Dropped award tiers:** removed Bronze / Merit / Certificate everywhere (Marco doesn't want to anchor the jury low; TDC Annual is the highest) — replaced the detailed bottom awards list with the `.cs-honor` headline + a tier-free `.cs-reco` "Recognised at" row (ADC New York · TDC · ADC Germany logos); JSON-LD `award[]` simplified to match. (3) **Specimen video → 4:5:** `.specimen-frame` `aspect-ratio` 9/16→4/5 (CSS `object-fit:cover` crop, no re-encode) so the glyphs read larger; bumped `.specimen` max-width to `min(460px,84vw)`. (4) **Full-width scrub:** moved the hover-scrub listener from the frame to `window` — scrubs whenever the cursor's Y is in the video's vertical band at ANY X (incl. beside the video); leaving the band resumes the loop. (5) **Discreet scrub line:** 2px+glow → 1px `rgba(255,255,255,.45)` no glow, toggled via `.is-scrubbing`. Committed/pushed without full preview re-verification at Marco's request.
- **2026-06-19**: `/500ls/` — **interleaved the interactive elements between the campaign images** (Marco's requested media order). Merged the old `#visuals` gallery and `#tester` sections into one `#visuals` flow: case film → board `04` + 2-up (`02`/`01`) → specimen video → board `06` + 2-up (`05`/`07`) → type tester → board `08` + 2-up (`09`/`10`) → springs legend. Split the single 9-image `.gallery` into three groups and gave `.gallery` a `margin-top` for even spacing. Dropped the now-redundant "The parametric system" section heading + lead; moved `id="tester"` onto the `.tt` element so the header "Try the typeface ↓" anchor still resolves. Verified via preview the DOM order matches the spec, tester anchor works, specimen is 4:5; pushed at Marco's request.
- **2026-06-19**: Fixed black "Blitzer" (flashes) at the top/bottom edge of the specimen video — the `.specimen-frame` background was `#000`, which flickered through at the rounded edges during scrub/repaint. Set the frame background to Cloud Dancer `#F0EEE9` (matches the page + the video's own bg) and added `transform: scale(1.04)` on the `<video>` to over-cover and kill any sub-pixel hairline.
- **2026-06-19**: Specimen video is now **scrub-only on mouse devices** (Marco: "only play while sliding over it, otherwise stay where it is"). Removed `autoplay`/`loop` from the `<video>`; on fine-pointer it loads paused on the first frame and only moves while the cursor is in its vertical band, freezing on the current frame when the cursor leaves (no auto-loop). Touch/coarse-pointer still loops (JS `v.loop=true` + play + IntersectionObserver pause off-screen). Verified via preview: paused on load, doesn't advance on its own, scrubs top=0.04s→bottom=59.6s, freezes after leave, no console errors.
- **2026-06-19**: Tamed the case-study tester's **baseline wave** (Marco: 100% looked chaotic, "up to 50% is aesthetic"). Halved the amplitude — `tt-script` now uses `amp=(+wave.value)/200` so the slider still reads 0–100% but 100% = the old 50% intensity. Bumped the default from 15%→**30%** so the on-load wave looks unchanged (30% on the new scale = old 15% amplitude). Only `500ls/index.html` changed; the live tester in `index.html` is untouched.
- **2026-06-24**: Two case-study tester fixes. (1) **Caption no longer jumps on Shuffle:** different cuts have different metrics/widths so the preview's height varied per shuffle and, because `.tt-stage` vertically-centred preview+caption together, the caption hopped. Reworked `.tt-stage` to a fixed-height grid (`height:clamp(220px,30vw,360px); grid-template-rows:minmax(0,1fr) auto`) with the preview centred in row 1 (`align-self:center; overflow:visible`) and the caption pinned in row 2 — so the caption's Y is constant (verified: caption-top range 0 across 18 shuffles × multiple words at 390px & 1200px). (2) Added the **glyph count** to the springs legend heading: "13 springs · 13 cuts · 13 colours · **1,300 glyphs**" (13 cuts × 100 glyphs/cut, from the OTF `maxp` numGlyphs).
- **2026-06-25**: Built the **Wave lab** interactive section (`#wavelab`) on `/500ls/`, right after the board, showing the board's data→wave→grid→letter derivation with the real assets. Generated 13 clean web SVGs (`assets/05_500ls/waves/01…13.svg`, integer-coord single polygons, 374 KB→79 KB) from Marco's source waveforms; embedded the 13 springs' real chemistry (from the CSV) + the board's value→parameter mapping. Per Marco's final brief: **only the 13 real springs, no free sliders**, but you still learn what each value does by **hovering a data row → the matching feature is annotated on the real wave** (period/amplitude brackets via a threshold peakdet on the polygon top edge, edge/thickness highlights). "Stack into grid" tiles the wave + draws the raster (verticals at peaks/troughs, horizontals every 3rd wave); "Drop a letter" places the spring's real cut glyph into the grid (letters not morphed — fonts are static, but each cut genuinely fits its own spring's grid). Strokes use `vector-effect:non-scaling-stroke` for crisp lines at any scale. Verified via preview: 13 waves fetch + recolour to exact brand hex, hover annotations + captions work, grid shows verticals+horizontals for calm & busy springs alike (fixed a peakdet miss on low-amplitude waves), letter renders in `lsNN`, responsive 390px (chips wrap, panel stacks), no console errors. Source `SVG_Wellen/` + CSV left untracked (kept off GitHub Pages to stay lean).
- **2026-06-25**: Wave-lab refinements per Marco (+ his `Beispiel_SVG/` reference). (1) **Default = 3 stacked waves** (upper/middle/lower) with the **spacing driven by Sulfate** — the spacing is itself a datapoint. (2) Adopted the **canonical brandbook mapping** from `beispiel-beschriftung.svg`: Na→Frequency, Cl→Amplitude, Ca→Crest shape (expOut), Mg→Trough shape (expIn), CO₂→Band thickness, **SO₄→Wave spacing** (was "phase"), Temp→Colour; hover captions use the brandbook wording. (3) Grid is now a **DIN-A portrait sheet** (`3000×4243`, 1:√2 — "we always lay the grid on DIN-A") — much taller. (4) **Filigree** annotations: `#2b2b2a`, hairline `non-scaling-stroke`, smaller labels (matching the 0.25px/8px brandbook style). (5) **Letter fits the grid properly**: scaled via `getBBox` to exactly **6 grid-rows tall** (verified ratio 1.00) and snapped, instead of an em-based guess. Verified via preview (desktop + 390px): 3-wave default, SO₄ spacing annotation, DIN-A grid, 6-row letter, no console errors.
- **2026-06-25**: Wave-lab spacing fix. The source `SVG_Wellen/*.svg` each already contain **3 stacked waves** (exact translated copies) with a per-spring vertical pitch (HS 120 → KA 40, tracking SO₄) — I'd only extracted the first wave and *guessed* the gap. Now the **real measured pitch** is stored per spring (`p:` in the data) and used both for the 3-wave default AND carried straight into the DIN-A grid tiling (so the spacing continues into the grid). Also fixed the SO₄ "Spacing" bracket to measure top-to-top (= pitch) and reserved `.wl-cap` min-height (`3.2em`) so the "Stack into grid" button no longer jumps when a hover caption wraps to two lines. (Committed at Marco's request without a fresh preview pass.)
